{
  "name": "@htybox/link",
  "version": "0.1.0",
  "description": "HtyBox 双端连接协议 TS 绑定（消息类型 / 帧编解码 / E2E / DaemonClient）",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "smoke:connect": "tsx scripts/ws-smoke-connect.ts"
  },
  "dependencies": {
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "@types/node": "^22.10.0",
    "ws": "^8.18.0",
    "tsx": "^4.19.0"
  }
}
